provider "aws" {
    region = var.awsRegion
    #Ran this from Windows and it gave me a bit of trouble with external keys, so if you have some issues you can just input yours here
    #access_key = ""
    #secret_key = ""
}
